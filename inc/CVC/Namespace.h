// Compatibility shim: forward to libcvc namespace (Phase 8)
// CVC_NAMESPACE = cvc (from libcvc).  Old CVC:: namespace aliases are
// provided in the individual compat headers (Types.h, App.h, etc.).
#include <cvc/namespace.h>
